// Shared form-input class (used on input/select/textarea across the forms).
// A design-system token, not a deep module — see issue #21.
export const fieldClass = "w-full border border-zinc-300 px-3 py-2 text-sm";
